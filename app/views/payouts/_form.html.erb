<%= form_with model: payout do |form| %>
  <% if payout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payout.errors.count, "error") %> prohibited this payout from being saved:</h2>

      <ul>
        <% payout.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount, step: :any %>
  </div>

  <div class="field">
    <%= form.label 'Foregin Expense' %>
    <%= form.radio_button :foreign, 'true' %>
    <%= form.label :foreign, 'True' %>
    <%= form.radio_button :foreign, 'false' %>
    <%= form.label :foreign, 'False' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
